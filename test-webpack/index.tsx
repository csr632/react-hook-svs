import * as React from "react";
import * as ReactDOM from "react-dom";

import App from "../src/demos/abstraction";

var mountNode = document.getElementById("app");
ReactDOM.render(<App />, mountNode);
